---
deployment:
  tasks:
    - export DEPLOYPATH=/home/YOUR_CPANEL_USERNAME/public_html
    - /bin/cp -R index.html $DEPLOYPATH/
    - /bin/cp -R methodology.html $DEPLOYPATH/
    - /bin/cp -R projects.html $DEPLOYPATH/
    - /bin/cp -R css $DEPLOYPATH/
    - /bin/cp -R photos $DEPLOYPATH/
